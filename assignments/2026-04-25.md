# 2026-04-25 · Week 2 · Sat

**Theme:** HTTP & REST + Udemy start

## Questions

1. For your `Note` resource: list routes and who may call them (public vs auth).
2. Draw the request body for `POST /notes` — required vs optional fields.

---

## Answers

1. Note routes

```
GET /notes -> auth users
GET /notes/:noteId -> auth users
POST /notes -> owner
PUT /notes/:noteId -> owner
DELETE /notes/:noteId -> owner
```

2.

```ts
interface CreateNoteDto {
  title: string;
  content: string;
  priority?: "high" | "medium" | "low";
}
```

### Reviewer comments

- **Q1:** Routes plus caller rules are clear: authenticated users for collection/read, owner for create/update/delete paths you chose. Optional tighten: if notes are private per user, consider `GET /notes/:noteId` as **owner only** (not any authenticated user) unless you intend shared notes.
- **Q2:** Strong answer: `CreateNoteDto` with required `title`/`content` and optional `priority` union matches how Nest DTOs and validation will look next.

---

- [x] **Assignment done** (written answers in your notes or repo — calendar day counts complete only with this checked)

_Back to [daily track](../05-daily-track-24-weeks.md#daily-assignments-by-calendar-day)._
