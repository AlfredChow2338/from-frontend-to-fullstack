# 2026-04-26 · Week 2 · Sun

**Theme:** HTTP & REST + Udemy start

## Questions

1. What did you create in `udemy-zero-to-hero` today, and how does it differ from `portfolio-api`?
2. What is `nest new` doing on disk (folders you care about)?

---

## Answers

1. I created a set of task management API and services with auth service. I don't know what is portfolio-api.

2. It creates a complete new Nestjs project and install the dependencies required. It populates all the standard files such as src test config.

### Reviewer comments

- **Q1:** Good concrete progress summary for `udemy-zero-to-hero` (tasks + auth). It is okay not to know `portfolio-api` yet; for now, treat it as your separate main roadmap repo while `udemy-zero-to-hero` is course practice code.
- **Q2:** Correct high-level understanding: `nest new` scaffolds a full project and installs dependencies. To deepen, mention key generated areas like `src/`, `test/`, config files, and starter `main.ts`/`app.module.ts`.

---

- [x] **Assignment done** (written answers in your notes or repo — calendar day counts complete only with this checked)

_Back to [daily track](../05-daily-track-24-weeks.md#daily-assignments-by-calendar-day)._
