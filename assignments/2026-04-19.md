# 2026-04-19 · Week 1 · Sun

**Theme:** Node fundamentals

## Questions

1. Pick one line from a stack trace you saw: what file/function did it point to, and what did you change?
2. What rule did ESLint enforce that you fixed, and why might that rule exist?

---

## Answers

1. ReferenceError: window is not defined at Object.<anonymous> (/usr/app/src/utils/auth.js:12:18)

```js
// From this:
const token = window.localStorage.getItem('auth');

// To this:
const token = typeof window !== 'undefined' ? window.localStorage.getItem('auth')
```

2. The no-unused-vars rule is a "code hygiene".

This ensure variables created but not used is a waste of memory and cofusing.

### Reviewer comments

- **Q1:** Strong practical example: you identified the exact stack-trace location and made a platform-safe fix for server-side runtime where `window` is unavailable. Nice debugging flow.
- **Q2:** Correct idea. `no-unused-vars` helps readability and prevents dead code/forgotten refactors. It is less about memory and more about maintainability, bug prevention, and cleaner reviews.

---

- [x] **Assignment done** (written answers in your notes or repo — calendar day counts complete only with this checked)

_Back to [daily track](../05-daily-track-24-weeks.md#daily-assignments-by-calendar-day)._
